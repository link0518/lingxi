## [Unreleased]

### Added

- 增加“用户消失后 AI 主动关心”功能：仅对用户最新会话生效，按 12/24/48 小时三档触发并限制最多 3 次，用户一旦发言即重置。
- 服务端新增幂等与配置持久化：`idle_nudges` 用于去重与次数控制，`llm_settings` 用于保存每个用户的 LLM 配置（含轻量任务模型），后台任务使用轻量模型生成台词。
- 影响文件：`server/index.js`、`server/db.js`。

